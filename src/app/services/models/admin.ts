// Update the Admin interface to include all required properties
export interface Admin {
  id: string;
  email: string;
  password: string;
  name: string;
  telephone: string; // Ensure this property is included
}
